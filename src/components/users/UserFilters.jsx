export default function UserFilters({ search, setSearch, status, setStatus, type, setType, verified, setVerified, onReset, onAdd }) {
  return <div className="users-toolbar"><div className="user-search">⌕<input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search users by name, email or mobile..." /></div><select value={status} onChange={(event) => setStatus(event.target.value)} aria-label="Filter by status"><option>All Status</option><option>Active</option><option>Inactive</option></select><select value={type} onChange={(event) => setType(event.target.value)} aria-label="Filter by user type"><option>All User Type</option><option>Customer</option><option>Delivery Partner</option></select><select value={verified} onChange={(event) => setVerified(event.target.value)} aria-label="Filter by verification"><option>All Verified</option><option>Verified</option><option>Not Verified</option></select><button type="button" className="add-user-button" onClick={onAdd}>＋ Add User</button><div className="joined-date"><small>Joined Date</small><button type="button">▣　12 May 2024　-　12 May 2024</button></div><button type="button" className="reset-users" onClick={onReset}>↻　Reset</button></div>;
}
